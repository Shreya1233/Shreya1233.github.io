function goToPage(page) {
  window.location.href = page; // Redirects to the given page within your website
}

function openInNewTab(url) {
  window.open(url, '_blank'); // Opens the given URL in a new tab
}